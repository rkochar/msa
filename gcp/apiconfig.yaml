swagger: '2.0'
info:
  title: API Config
  description: API Config
  version: 1.0.0
schemes:
- https
produces:
- application/json
paths:
  /publish:
    get:
      summary: lambda that will publish param query and header head to a mq topic
      operationId: /publish
      x-google-backend:
        address: https://europe-west1-master-thesis-faas-monad.cloudfunctions.net/publish
      responses:
        '200':
          description: TODO
          schema:
            type: string
